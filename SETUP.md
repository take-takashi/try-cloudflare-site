# Setup command

```bash
$ touch .mise.local.toml
$ nvim .mise.local.toml
$ cat .mise.local.toml
[tools]
"npm:ntn" = { version = "latest", depends = ["node"] }

[env]
NOTION_API_TOKEN = "ntn_****"

```

## `AGENTS.md` を作成

- grill-meを用いて作成。
