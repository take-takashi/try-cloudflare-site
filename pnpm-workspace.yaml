packages:
  - apps/*
  - packages/*

allowBuilds:
  esbuild: true
  sharp: true
  workerd: true

minimumReleaseAge: 10080

# @astrojs/check -> yaml-language-server が脆弱な yaml を固定しているため、
# 上流更新で解消できるまで patched version に差し替える。
overrides:
  yaml: 2.9.0
